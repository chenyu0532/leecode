38题：垃圾题目，放弃


1. Map结构有极快的查找速度
2. 字符串转数组：str.split('xxx')
    数组转字符串:  array.join('xxxx')
    字符串删除第一个特定字符：str.replace("z", "");
3. 数组 字符串中的includes和indexOf，作用在大部分情况是一样的
4. 字符串是不可变的，所以一切的交换数值等方法都是没有作用的，字符串的方法
     只有get而没有set，如果想改变字符串，那么要先转化为数组，改变后，再转为字符串
     replace
5. 438 是一个类型的，总结下

splice
indexOf
